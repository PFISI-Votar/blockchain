# Sepolia testnet deployment — NEVER commit the real .env
# RPC endpoint (Infura / Alchemy / public node)
SEPOLIA_RPC_URL=

# Deployer EOA private key (funded with Sepolia ETH). Without 0x prefix is fine.
PRIVATE_KEY=

# DEFAULT_ADMIN_ROLE holder: Multisig (Gnosis Safe) or Governor address.
# Only this account can grant/revoke roles after deployment.
ADMIN_MULTISIG_ADDRESS=
